// Checks if two integers are equal.
// - x: The 1st integer.
// - y: The 2nd integer.
// = True if x and y are equal, False otherwise.
Int/eq
: ∀(x: Int)
  ∀(y: Int)
  Bool
= λ{
  #Pos: λx.value λ{
    #Pos: λy.value (Nat/eq x.value y.value)
    #NegSuc: λy.value #False{}
  }
  #NegSuc: λx.value λ{
    #Pos: λy.value #False{}
    #NegSuc: λy.value (Nat/eq x.value y.value)
  }
}