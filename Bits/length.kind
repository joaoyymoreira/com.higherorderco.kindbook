// Returns the length of a Bits structure, that is, how many bits it has.
Bits/length
: ∀(bits: Bits/Bits)
  Nat/Nat
= λ{
  #E: #Zero{}
  #O: λbits.tail #Succ{(Bits/length bits.tail)}
  #I: λbits.tail #Succ{(Bits/length bits.tail)}
}
