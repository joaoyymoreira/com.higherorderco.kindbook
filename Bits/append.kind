// Appends two Bits values.
// - as: The 1st Bits value.
// - bs: The 2nd Bits value.
// = A new Bits value representing the concatenation of as and bs.
Bits/append
: ∀(as: Bits/Bits)
  ∀(bs: Bits/Bits)
  Bits/Bits
= λ{
  #E: λbs bs
  #O: λas.tail λbs #O{(Bits/append as.tail bs)}
  #I: λas.tail λbs #I{(Bits/append as.tail bs)}
}
