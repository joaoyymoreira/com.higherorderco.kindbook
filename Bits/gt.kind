// Checks if one Bits value is greater than another.
// - a: The first Bits value.
// - b: The second Bits value.
// = True if a > b, False otherwise.
Bits/gt
: ∀(a: Bits)
  ∀(b: Bits)
  Bool
= λ{
  #E: λ{
    #E: #False{}
    #O: λb.tail #False{}
    #I: λb.tail #False{}
  }
  #O: λa.tail λ{
    #E: #True{}
    #O: λb.tail (Bits/gt a.tail b.tail)
    #I: λb.tail #False{}
  }
  #I: λa.tail λ{
    #E: #True{}
    #O: λb.tail #True{}
    #I: λb.tail (Bits/gt a.tail b.tail)
  }
}