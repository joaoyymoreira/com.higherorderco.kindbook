// Defines an Ord tree datatype.
// - K: Key type.
// - V: Value type.
data OrdTree (K: *) (V: *) {
  #Leaf
  #Node{
    key_value: (Pair K V)
    //balance: Bool/Balance -- FIXME
    left: (OrdTree K V)
    right: (OrdTree K V)
  }
}
