// Computes the length of a list.
// - A: The type of elements in the list.
// - xs: The input list.
// = The number of elements in the list.
List/length
: ∀(A: *)
  ∀(xs: (List/List A))
  Nat/Nat
= λA λ{
  #Nil: #Zero{}
  #Cons: λxs.head λxs.tail #Succ{(List/length _ xs.tail)}
}
