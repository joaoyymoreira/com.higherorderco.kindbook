// Reverses the order of elements in a list.
// - 1st: The type of elements in the list.
// - 2nd: The input list to be reversed.
// = A new list with the elements in reverse order.
List/reverse
: ∀(A: *)
  ∀(xs: (List/List A))
  (List/List A)
= λA λxs (List/foldl _ _ (λxs λx #Cons{x xs}) #Nil{} xs)
