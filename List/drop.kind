// Drops the first n elements from a list.
// - A: The type of elements in the list.
// - n: The number of elements to drop.
// - xs: The input list.
// = The list without its first n elements.
List/drop
: ∀(A: *)
  ∀(n: Nat/Nat)
  ∀(xs: (List/List A))
  (List/List A)
= λA λ{
  #Zero: λxs xs
  #Succ: λn.pred λ{
    #Nil: #Nil{}
    #Cons: λxs.head λxs.tail (List/drop A n.pred xs.tail)
  }
}
