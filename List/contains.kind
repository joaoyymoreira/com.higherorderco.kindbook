// Checks if a list contains a specific element.
// - A: Element type.
// - e: Equality function for A.
// - x: Element to search for.
// - xs: List to search in.
// = True if found, False otherwise.
List/contains : ∀(A: *) (Trait/Eq A) -> A -> (List A) -> Bool
| A e x #Nil             = #False
| A e x #Cons{head tail} = (Bool/or (Trait/Eq/eq A e x head) (List/contains A e x tail))
