// Right-folds a list, combining elements with a function.
// Equivalently, transforms a list on its Church encoding.
// - A: Type of elements.
// - xs: The list to fold.
// - P: Result type.
// - nil: Initial value.
// - cons: Combining function.
// = The final result.
List/fold : ∀(A: *) ∀(P: *) (List A) -> P -> (A -> P -> P) -> P
| A P #Nil             nil cons = nil
| A P #Cons{head tail} nil cons = (cons head (List/fold A P tail nil cons))