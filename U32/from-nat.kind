// Converts a natural number to a 32-bit unsigned integer.
// - x: The natural number to convert.
// = The U32 representation of the input natural number.
U32/from-nat : Nat -> U32
| #Zero    = 0
| #Succ{n} = (+ 1 (U32/from-nat n))

#test: (U32/from-nat #Zero) == 0
#test: (U32/from-nat #Succ{#Succ{#Zero}}) == 2
