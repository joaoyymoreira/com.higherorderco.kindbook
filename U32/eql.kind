// Compares two U32 values for equality.
// - a: The first U32 value to compare.
// - b: The second U32 value to compare.
// = Returns 1 if the values are equal, 0 otherwise.
U32/eql : U32 -> U32 -> U32
= λa λb (== a b)

#test: (U32/eql 5 5) == 1
#test: (U32/eql 5 3) == 0
