// Converts a U32 to a Natural number.
// - x: The U32 value to convert.
// = The Natural number equivalent of the input U32 value.
U32/to-nat : U32 -> Nat
= λx if (U32/to-bool x) {
  #Succ{(U32/to-nat (- x 1))}
} else {
  #Zero
}

#test: (U32/to-nat 0) == #Zero
#test: (U32/to-nat 2) == #Succ{#Succ{#Zero}}
