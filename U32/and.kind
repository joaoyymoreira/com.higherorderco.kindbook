// Performs bitwise AND operation on two U32 values.
// - a: The first U32 value.
// - b: The second U32 value.
// = The result of bitwise AND operation between the two input values.
U32/and : U32 -> U32 -> U32
= λa λb (& a b)

#test: (U32/and 5 3) == 1
#test: (U32/and 15 7) == 7
