// Calculates the modulo (remainder) of two 32-bit unsigned integers.
// - a: The dividend.
// - b: The divisor.
// = The remainder of the division as a U32 value.
U32/mod : U32 -> U32 -> U32
= λa λb (% a b)

#test: (U32/mod 7 3) == 1
#test: (U32/mod 10 5) == 0
