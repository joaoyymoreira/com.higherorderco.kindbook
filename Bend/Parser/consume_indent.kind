// Consumes exactly the given number of indentation spaces.
// - exp: Expected number of spaces to consume.
// = 0 if successful, or an error message otherwise.
Bend/Parser/consume_indent : U64 -> (Parser U64)
| exp = do Parser {
  ask got = (Bend/Parser/skp_newlines)
  if (U64/eq exp got) {
    (Parser/pure _ 0)
  } else {
    (Parser/fail _ (String/concat ["Expected " (U64/to_string exp) " spaces, got " (U64/to_string got)]))
  }
}
