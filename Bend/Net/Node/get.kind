Bend/Net/Node/get : Bend/Net/Node -> Nat -> (Maybe Bend/Net/Port)
| #Node{main aux1 aux2 _} n =
  switch n {
    0: #Some{main}
    _{n}: switch n {
      0: #Some{aux1}
      _{n}: switch n {
        0: #Some{aux2}
        _{n}: #None
      }
    }
  }
