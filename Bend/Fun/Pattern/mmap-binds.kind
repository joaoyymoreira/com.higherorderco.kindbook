use Bend/Fun/Pattern as Pat

Bend/Fun/Pattern/mmap-binds :
  ∀(M: * -> *) (Monad M) -> ((Maybe String) -> (M (Maybe String))) -> Bend/Fun/Pattern -> (M Bend/Fun/Pattern)

| M #Monad{pure bind} f #Var{x} =
  (bind (Maybe String) Pat (f x) λx (pure Pat #Var{x}))

| M m f pat =
  (Bend/Fun/Pattern/mmap-children M m (Bend/Fun/Pattern/mmap-binds M m f) pat)
