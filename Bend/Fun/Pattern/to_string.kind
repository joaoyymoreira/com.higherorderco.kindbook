Bend/Fun/Pattern/to_string : Bend/Fun/Pattern -> String
| #Var{nam}     = (Bend/Fun/bnd_to_string nam)
| #Chn{nam}     = (String/append "$" nam)
| #Fan #Tup els = (String/concat ["(" (String/join ", " (List/map _ _ Bend/Fun/Pattern/to_string els)) ")"])
| #Fan #Dup els = (String/concat ["{" (String/join " " (List/map _ _ Bend/Fun/Pattern/to_string els)) "}"])
| #Ctr nam #Nil = (String/concat ["(" nam ")"])
| #Ctr nam els  = (String/concat ["(" nam " " (String/join " " (List/map _ _ Bend/Fun/Pattern/to_string els)) ")"])
| #Num val      = (Nat/to_string val)
| #Lst els      = (String/concat ["[" (String/join ", " (List/map _ _ Bend/Fun/Pattern/to_string els)) "]"])
| #Str val      = (String/concat ["\"" val "\""])
