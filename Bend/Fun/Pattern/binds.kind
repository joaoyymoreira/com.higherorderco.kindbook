// Extracts all bound variable names from a Pattern.
// - pat: The pattern to extract bindings from.
// = A list of all bound variable names in the pattern.
Bend/Fun/Pattern/binds : Bend/Fun/Pattern -> (List String)
| #Var{#Some{name}} = [name]
| pat               = (List/concat-map _ _ Bend/Fun/Pattern/binds (Bend/Fun/Pattern/children pat))
