// Bend/Fun/Pattern/mmap_binds

Bend/Fun/Pattern/mmap_binds <M: Type -> Type> (m: Monad M) (f: (Maybe String) -> M (Maybe String)) (pat: Bend/Fun/Pattern/Pattern) : (M Bend/Fun/Pattern/Pattern)
| m f (#Var x) =
  do m {
    ask x = (f x)
    return (#Var x)
  }
| m f pat =
  (Bend/Fun/Pattern/mmap_children m (Bend/Fun/Pattern/mmap_binds m f) pat)
