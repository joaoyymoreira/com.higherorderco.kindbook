use Bend/Fun/Type as Type

// Types of terms and functions in Bend.
data Bend/Fun/Type {
  #Any
  #Hol
  #Var{ nam: String }
  #Arr{ dom: Type   cod: Type }
  #Ctr{ nam: String arg: (List Type) }
  #Tup{ els: (List Type) }
  #U24
  #I24
  #F24
  #Non
  #Num{ typ: Type }
  #Int{ typ: Type }
}
