// A pattern matching rule in a function definition
data Bend/Fun/Rule {
  #Rule {
    pat : (List Bend/Fun/Pattern)  // Patterns to match against
    bod : Bend/Fun/Term            // Body of the rule
  }
}
