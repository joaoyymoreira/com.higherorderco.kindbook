// Applies the to-string function of a Show instance.
// - A: Input type.
// - s: Show instance for A.
// - x: Value to convert to string.
// = String representation of x.
Show/to-string : ∀(A: *) ∀(s: (Show A)) A -> String
| A #Show{to-string} x = (to-string x)

#test: (Show/to-string Bool Bool/Show #True) == "True"
#test: (Show/to-string Bool Bool/Show #False) == "False"
#test: (Show/to-string Nat Nat/Show #0) == "0"
#test: (Show/to-string Nat Nat/Show #3) == "3"