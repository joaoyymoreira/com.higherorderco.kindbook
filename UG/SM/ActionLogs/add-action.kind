// Adds an action to ActionLogs at a specific tick.
// - A: Action type.
// - logs: Original ActionLogs.
// - t: Tick to add the action.
// - a: Action to add.
// = New ActionLogs with action added.

// FIX: log is now a reserved keyword
UG/SM/ActionLogs/add-action : ∀(A: *) (UG/SM/ActionLogs A) -> UG/SM/Tick -> A -> (UG/SM/ActionLogs A)
| A lo t a = lo
