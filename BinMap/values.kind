// Converts a BinMap to a List of the stored values.
// None values are skipped.
// - A: The type of values stored in the map.
// - m: The input BinMap.
// = A list with the values stored in the BinMap.
BinMap/values
: ∀(A: *)
  ∀(m: (BinMap/BinMap A))
  (List/List A)
= λA λ{
  #Leaf: #Nil{}
  #Node: λ{
		#None: λlft λrgt (List/append A (BinMap/values A lft)  (BinMap/values A rgt))
		#Some: λv λlft λrgt #Cons{v (List/append A (BinMap/values A lft) (BinMap/values A rgt))}
	}
}
  
