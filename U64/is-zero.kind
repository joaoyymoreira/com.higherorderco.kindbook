// Checks if a U64 is zero.
// - x: The U64 to check.
// = True if the input is zero, False otherwise.
U64/is-zero : U64 -> Bool
= λ{
  0: #True
  _{p}: #False
}

#test: (U64/is-zero 0) == #True
#test: (U64/is-zero 1) == #False
#test: (U64/is-zero 42) == #False
