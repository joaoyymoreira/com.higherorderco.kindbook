// Doubles a given U64 value.
// - x: The U64 value to be doubled.
// = The input value multiplied by 2.
U64/double : U64 -> U64
= λx (U64/mul x 2)

#test: (U64/double 5) == 10
#test: (U64/double 0) == 0
