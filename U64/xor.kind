// Performs bitwise XOR operation on two U64 values.
// - a: The first U64 value.
// - b: The second U64 value.
// = The result of bitwise XOR operation between the two input values.
U64/xor : U64 -> U64 -> U64
= λa λb (^ a b)

#test: (U64/xor 5 3) == 6
#test: (U64/xor 12 5) == 9
