// Performs bitwise OR operation on two U64 values.
// - a: The first U64 value.
// - b: The second U64 value.
// = The result of bitwise OR operation between the two input values.
U64/or : U64 -> U64 -> U64
= λa λb (| a b)

#test: (U64/or 5 3) == 7
#test: (U64/or 12 5) == 13
