// Replicates a char n times.
// - c: Char to replicate.
// - n: Number of repetitions.
// = String with n repetitions of c.
String/replicate : Char -> Nat -> String
| c #Zero    = ""
| c #Succ{n} = (String/append (String/from-char c) (String/replicate c n))

#test: (String/replicate 'a' #0) == ""
#test: (String/replicate 'a' #3) == "aaa"
