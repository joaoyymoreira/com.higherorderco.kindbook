// Left-folds a string.
// - A: Accumulator type.
// - s: Input string.
// - z: Initial value.
// - f: Combining function.
// = Folded result.
String/foldl : ∀(A: *) String -> A -> (A -> Char -> A) -> A
| A s z f = (List/foldl A Char s z f)
