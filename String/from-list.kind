// Converts a list of characters to a string.
// - chars: Input list of characters (U64 values).
// = String composed of the characters in the list.
String/from-list : (List U64) -> String
| chars = chars

#test: (String/from-list "abc") == "abc"
#test: (String/from-list "") == ""
